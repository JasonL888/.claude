# mindmap — Topic Hierarchies

```
mindmap
  root((Machine Learning))
    Supervised
      Classification
      Regression
    Unsupervised
      Clustering
      Dimensionality Reduction
    Reinforcement
      Q-Learning
      Policy Gradient
```

Indent = hierarchy level. `root((...))` = circle, `root[...]` = rect, `root(((...)))` = double circle. No `%%{init}%%` support.
